# If you run the API locally (not via Docker), copy to `.env` and adjust.
DATABASE_URL=postgresql://postgres:postgres@localhost:15432/airquality?schema=public
PORT=4000
CORS_ORIGIN=http://localhost:5173
